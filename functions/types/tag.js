export default {
  id(root) {
    return root.id;
  },
  count(root) {
    return root.count;
  },
  createdAt(root) {
    return root.createdAt;
  },
  name(root) {
    return root.name;
  },
  updatedAt(root) {
    return root.updatedAt;
  },
};
